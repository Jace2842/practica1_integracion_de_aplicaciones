# CRM Service - Fresh & Go

Servicio de gestión de relaciones con clientes (CRM) para el sistema Fresh & Go.

## Endpoints

- `GET /` - Información del servicio
- `GET /health` - Health check
- `GET /clientes` - Listar clientes
- `GET /clientes/:id` - Obtener cliente por ID
- `GET /pedidos` - Listar pedidos
- `GET /pedidos/:id` - Obtener pedido por ID
- `GET /proveedores` - Listar proveedores
- `GET /proveedores/:id` - Obtener proveedor por ID
- `GET /conductores` - Listar conductores
- `GET /conductores/:id` - Obtener conductor por ID

## Instalación

```bash
npm install
```

## Ejecución

```bash
npm start
```

Puerto: 3001
